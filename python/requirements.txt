# audiocraft pulls in a compatible torch/torchaudio transitively, but we pin
# the top-line versions explicitly so CUDA and MPS installs line up. Torch
# itself is installed by scripts/install-local.mjs with the right --index-url
# for the target platform (default PyPI for macOS/MPS, pytorch.org/cu121 for
# Linux/Windows + CUDA).
audiocraft==1.3.0
